services:
  - type: web
    name: app-dist-carga-backend
    env: node
    plan: free
    rootDir: .
    buildCommand: npm ci --production
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production 