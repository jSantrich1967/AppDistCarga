services:
  - type: web
    name: app-dist-carga-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start --prefix backend
    envVars:
      - key: NODE_ENV
        value: production 