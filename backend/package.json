{
  "name": "backend-redirect",
  "version": "1.0.0",
  "description": "Redirect to root package.json for Render compatibility",
  "main": "../server.js",
  "scripts": {
    "start": "cd .. && npm start",
    "build": "cd .. && npm install",
    "postinstall": "cd .. && npm install"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "express-validator": "^7.0.1",
    "cookie-parser": "^1.4.6"
  }
} 